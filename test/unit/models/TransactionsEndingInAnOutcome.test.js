'use strict'
/* global describe, it */

const assert = require('assert')

describe('TransactionsEndingInAnOutcome Model', () => {
  it('should exist', () => {
    assert(global.app.api.models['TransactionsEndingInAnOutcome'])
  })
})
